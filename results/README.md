# Results package

This directory contains the supplied analysis outputs and the locations where
new outputs are written. It is distributed with `data/` as the Zenodo data
package; the code-only Git release excludes its contents through `.gitignore`.

Results are grouped by chapter under `results/Code_chapterXX/`. Running a
script may overwrite files in its corresponding chapter directory.
